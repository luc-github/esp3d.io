---
title: "Conventions"
---

# API Conventions

## Request format

Most API endpoints accept both `GET` and `POST` requests.

## Response format

Responses can be in plain text or JSON depending on the `json` parameter.

## Authentication

When authentication is enabled, most endpoints require a valid session or password.

<aside class="warning-panel" data-icon="alert">
  <h4>Pre-alpha documentation</h4>
  <p>Convention details may change as ESP3D-X is still in active development.</p>
</aside>
